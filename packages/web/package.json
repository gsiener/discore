{
  "name": "@scorebot/web",
  "version": "1.0.0",
  "description": "Web interface for scorebot",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=scorebot",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@scorebot/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "vitest": "^1.2.0",
    "jsdom": "^24.0.0",
    "wrangler": "^4.50.0"
  }
}
